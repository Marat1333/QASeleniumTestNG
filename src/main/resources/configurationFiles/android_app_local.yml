#---------------------------------
#       Android app settings
#---------------------------------
settings:
  platform: android_phone
  browser: android_app
  host: http://0.0.0.0:4723
  #build: 1.0.0 # Build version for NativeApps or run id for browser tests
  timeout: 30 # default wait timeout in seconds

  # /Users/xhqautomation/node_modules/appium/node_modules/appium-chromedriver/chromedriver/mac/chromedriver

  #---------------------------------
  #              mobile
  #---------------------------------

capabilities:
  android_phone:
    android_app:
      platformName: Android
      autoDismissAlerts: true
      autoGrantPermissions: true
      autoAcceptAlerts: true
      deviceOrientation: portrait
      automationName: uiautomator2
      newCommandTimeout: 60000
      #deviceName: Use VM option -DmDeviceName
      #app: Use VM option -DmApp
      #platformVersion: 10.0 # not required option

    android_browser:
      browserName: chrome
      deviceName: Nexus 5 #samsung SM-A207F
      platformVersion: '10.0' #'9.0'
      platformName: Android
      automationName: uiautomator2
      newCommandTimeout: 60000
      deviceOrientation: portrait